[build]
  publish = "recruit-assist-web"
  # No build command needed - this is a static HTML/CSS/JS site

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

